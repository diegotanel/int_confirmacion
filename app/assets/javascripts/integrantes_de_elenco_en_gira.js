// Generated by CoffeeScript 1.6.2
(function() {
  jQuery(function() {
    var localidades, llenarLocalidades;
    var type
    if ($('#actor_type').length) { type = 'actor' }
    if ($('#director_type').length) { type = 'director' }
    if ($('#tecnico_type').length) { type = 'tecnico' }
    if (type != null) {
    var integrante_id = type.concat("_localidad_id");
    }
    
    llenarLocalidades = function(localidades) {
      var provincia, options;

      provincia = $('#ideec_provincia_id :selected').text();
      options = $(localidades).filter("optgroup[label='" + provincia + "']").html();
      if (options) {
        $("#" + integrante_id).html('<option value="">Seleccione una localidad...</option>');
        return $("#" + integrante_id).append(options);
      } else {
        return $("#" + integrante_id).empty();
      }
    };
    localidades = $("#" + integrante_id).html();
    llenarLocalidades(localidades);
    return $('#ideec_provincia_id').change(function() {
      return llenarLocalidades(localidades);
    });
  });

}).call(this);

  $(document).ready(function(){
    var type
    if ($('#actor_type').length) { type = 'actor' }
    if ($('#director_type').length) { type = 'director' }
    if ($('#tecnico_type').length) { type = 'tecnico' }
    if (type != null) {
    var integrante_id = type.concat("_fecha_de_nacimiento");
    }


    $("#" + integrante_id).datepicker({
      format: "dd/mm/yyyy",
      todayHighlight: true,
      todayBtn: 'linked',
      autoclose: true,
      language: "es",
      changeMonth: true,
      changeYear: true,
      yearRange: '1900:2020'
    });
  });
