// Generated by CoffeeScript 1.6.2
(function() {
  jQuery(function() {
    var provincias, llenarprovincias;

    llenarprovincias = function(provincias) {
      var region, options;

      region = $('#q_principal_localidad_provincia_region_id_eq :selected').text();
      options = $(provincias).filter("optgroup[label='" + region + "']").html();
      if (options) {
        $("#q_principal_localidad_provincia_id_eq").html('<option value="">Seleccione una provincia...</option>');
        return $("#q_principal_localidad_provincia_id_eq").append(options);
      } else {
        return $("#q_principal_localidad_provincia_id_eq").empty();
      }
    };
    provincias = $("#q_principal_localidad_provincia_id_eq").html();
    llenarprovincias(provincias);
    return $('#q_principal_localidad_provincia_region_id_eq').change(function() {
      return llenarprovincias(provincias);
    });
  });

}).call(this);
